---
'better-hooks': patch
---

Keep storage and media-query registries free of abandoned render state, rebind `useKeyPress` when its event list changes, and report media listener setup and cleanup failures through `onError`.
