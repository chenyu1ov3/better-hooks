# Internal Architecture Archive

The ADR files in this directory are repository records for maintainers. They
are intentionally outside `apps/docs/content` and are not part of the public
documentation navigation, search index, sitemap, or published package.
